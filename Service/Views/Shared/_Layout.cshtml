@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - InventorySystem</title>

    <link rel="stylesheet" href="~/css/Home.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/shared.css" asp-append-version="true" />
</head>
<body>
    <header class="header">
        <div class="header-top">
            <div class="logo">Inventory System</div>

            <div class="hamburger" id="hamburger">
                <span></span>
                <span></span>
                <span></span>
            </div>

            <div class="auth-buttons">
                @if (User.Identity.IsAuthenticated)
                {
                    <div style="display: flex; align-items: center; gap: 10px;">
                        <span style="font-weight: bold; color: #333;">@User.Identity.Name</span>
                        <form asp-controller="Account" asp-action="Logout" method="post" style="display:inline-flex; align-items:center;">
                            <button type="submit" class="btn-login">Выход</button>
                        </form>
                    </div>
                }
                else
                {
                    <a href="#" id="click-to-show" class="btn-login">Войти</a>
                }
            </div>
        </div>

        <div class="side-menu" id="sideMenu">
            <button class="close-btn" id="closeMenu">×</button>
            <nav class="mobile-nav">
                <a asp-controller="Home" asp-action="Index">Главная</a>
                <a asp-controller="Home" asp-action="SiteInformation">О нас</a>
                <a asp-controller="Home" asp-action="Functions">Функции</a>
                <a asp-controller="Home" asp-action="Contacts">Контакты</a>

                @if (User.Identity.IsAuthenticated)
                {
                    <div style="padding: 10px 20px; color: #333; font-weight: bold;">@User.Identity.Name</div>
                    <form asp-controller="Account" asp-action="Logout" method="post" style="width: 100%;">
                        <button type="submit" class="btn-login mobile-login" style="width: 100%; text-align: left; padding-left: 20px;">Выход</button>
                    </form>
                }
                else
                {
                    <a href="#" id="mobile-login-btn" class="btn-login mobile-login">Войти</a>
                }
            </nav>
        </div>

        <div class="header-middle">
            <img src="~/image/1.jpg" alt="Warehouse" class="header-image" />
        </div>

        <nav class="header-bottom">
            <ul class="menu">
                <li><a asp-controller="Home" asp-action="Index">Главная</a></li>
                <li><a asp-controller="Home" asp-action="SiteInformation">О нас</a></li>
                <li><a asp-controller="Home" asp-action="Functions">Функции</a></li>
                <li><a asp-controller="Home" asp-action="Contacts">Контакты</a></li>
            </ul>
        </nav>
    </header>

    <div class="body-container">
        @RenderBody()
    </div>

    <script src="~/js/shared_script.js" asp-append-version="true"></script>
    @RenderSection("Scripts", required: false)

    @await Html.PartialAsync("_LoginRegistrationPartial")
    @await Html.PartialAsync("_ConfirmEmailPartial") ```

    <script src="~/js/login_and_registration_script.js" asp-append-version="true"></script>
    <link rel="stylesheet" href="~/css/login_and_registration.css" asp-append-version="true" />

    <footer class="site-footer">
        <div class="footer-content">
            <div class="footer-left">
                <h3>Inventory System</h3>
                <p>Система учёта оборудования и инвентаря на складе. Автоматизация и контроль на всех этапах.</p>
            </div>

            <div class="footer-menu">
                <h4>Меню</h4>
                <ul>
                    <li><a asp-controller="Home" asp-action="Index">Главная</a></li>
                    <li><a asp-controller="Home" asp-action="SiteInformation">О нас</a></li>
                    <li><a asp-controller="Home" asp-action="Functions">Функции</a></li>
                    <li><a asp-controller="Home" asp-action="Contacts">Контакты</a></li>
                </ul>
            </div>

            <div class="footer-right">
                <h4>Контакты</h4>
                <p>Email: info@inventory-system.com</p>
                <p>Телефон: +373 (777) 77-777</p>
            </div>
        </div>

        <div class="footer-bottom">
            <p>© 2025 Inventory System — все права защищены.</p>
        </div>
    </footer>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
</body>
</html>